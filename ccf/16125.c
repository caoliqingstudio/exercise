//没有做完
#include <stdio.h>
int main(int argc, char const *argv[])
{
	int n,m;
	double p[20];
	int s[20];
	scanf("%d%d",&n,%m);
	int i;
	for (i = 0; i < n; ++i)
	{
		scanf("%lf",&p[i]);		
	}
	while(m--){
		for (i = 0; i < n; ++i)
		{
			scanf("%d",&s[i]);
		}
		for (i = 0; i < n; ++i)
		{
			
		}
	}
	return 0;
}